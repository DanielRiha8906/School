# hledejte optimalni model pro rady
#	  nhtemp, LakeHuron, airquality$Wind, discoveries, treering,
help(decomp)
# hledejte optimalni model pro rady
#	  nhtemp, LakeHuron, airquality$Wind, discoveries, treering,
help(decompose)
decomposition <- decompose(ldeaths)
plot decomposition
plot(decomposition)
ws <- ldeaths-decompose(ldeaths)$seasonal
plot(ws)
acf(ws)
pact(ws)
auto.arima(ldres, d=0, seasonal=F)
par(mfrow=c(2,1))
acf(residuals(ws),na.action = na.pass);pacf(residuals(ws),na.action = na.pass)
decomposition <- decompose(ldeaths)$seasonal
plot(decomposition)
ws <- ldeaths-decompose(ldeaths)$seasonal
plot(ws)
par(mfrow=c(2,1))
acf(residuals(ws),na.action = na.pass);pacf(residuals(ws),na.action = na.pass)
